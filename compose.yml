services :
    resulter_jdbc_db :
        image : 'postgres:16'
        env_file :
            - .env
        ports :
            - '5432:5432'
        volumes :
            - resulter_db_data:/var/lib/postgresql/data

volumes :
    resulter_db_data :
